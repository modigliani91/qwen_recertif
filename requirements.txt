# ==========================================================================
# Dépendances du pipeline d'extraction KYC (pipeline_extraction_kyc.ipynb)
# Installer dans cet ordre si vous constituez un environnement Domino dédié
# (le notebook installe lui-même ces mêmes versions via des cellules %pip).
# ==========================================================================

# --- 1) Traitement PDF et image -------------------------------------------
PyMuPDF==1.26.1
pillow==11.0.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# --- 2) Détection d'orientation des scans (OSD) ----------------------------
# Nécessite EN PLUS le binaire système `tesseract-ocr` (ex. apt-get install
# tesseract-ocr). Ce paquet Python ne suffit pas seul.
pytesseract==0.3.13

# --- 3) Rapports et structuration des résultats -----------------------------
pandas==2.2.3
openpyxl==3.1.5
tqdm==4.66.5

# --- 4) Moteur d'inférence du modèle Qwen (vision-langage, checkpoint FP8) --
# vLLM installe automatiquement une version de PyTorch compatible avec sa
# propre build CUDA : ne pas fixer `torch` séparément dans ce fichier, au
# risque de provoquer un conflit de résolution de dépendances.
vllm>=0.11.0

# transformers est une dépendance de vLLM ; on impose ce plancher pour
# garantir la reconnaissance de l'architecture Qwen3-VL (support ajouté en
# 4.57.0). Si votre checkpoint est une autre variante de Qwen-VL plus
# ancienne, cette contrainte reste compatible.
transformers>=4.57.0
